#
# remote auditlogs
#

# provides UDP syslog reception
# see docs/examples/ftylog.d/10-ftylog.cfg
module(load="imudp")
input(type="imudp" port="514")

# customize rsyslog message prepend (date, hostname, ...)
template(name="ForwardFormatMsg" type="string" string="%hostname% %msg%")

# forward msg to remote server
*.* @${RSYSLOG_SERVER_ADDRESS}:${RSYSLOG_SERVER_PORT};ForwardFormatMsg

# debug (log messages to file)
#*.* /var/log/debugfmt;RSYSLOG_DebugFormat
