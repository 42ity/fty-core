
==== poor man's arch  ====

COMPONENT       SOCK_TYPE       SOCK_URI
----------------------------------------
netmon          DEALER          (dbsock, filip)

db              ROUTER          (dbsock)

filip           ROUTER          (filip)
                DEALER          (dbsock)
                DEALER          (nmap)
                ROUTER          (nmap_reply)

nmap            ROUTER          (nmap)
                DEALER          (nmap_reply)

