#include <vector>
#include <string>
#include <string>

#include "dbhelpers.h"
#include "log.h"

void
    send_configure (
        std::vector<db_a_elmnt_t> rows,
        uint8_t action_type,
        const std::string &agent_name);
