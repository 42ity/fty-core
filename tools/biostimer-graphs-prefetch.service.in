[Unit]
Description=BIOS-Timer service that prefetches average computations for quicker graphs (implementation)
# This service only relies on REST API server directly
After=tntnet@bios.service
Requires=tntnet@bios.service

[Service]
Type=simple
ExecStart=@datadir@/@PACKAGE@/scripts/biostimer-graphs-prefetch.sh

[Install]
WantedBy=multi-user.target
