name: "CodeQL"

on:
  push:
    branches: [ "master", "release/IPM_Infra-1.3", "release/IPM_Infra-1.4", "release/IPM_Infra-1.5.0", "release/IPM-2.0.0", "release/IPM-2.0.1", "release/IPM-2.1.0", "release/IPM-2.2.0", "release/IPM-2.3.0", "release/IPM-2.3.1", "release/IPM-2.4.0", "release/IPM-2.5.0", "release/IPM-2.6.0", "release/IPM-2.7.0" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: "22 4 * * 1"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ python ]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}
          queries: +security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2
        with:
          category: "/language:${{ matrix.language }}"
